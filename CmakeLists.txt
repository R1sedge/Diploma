cmake_minimum_required(VERSION 3.10 FATAL_ERROR)

project(Water_simulation VERSION 0.1.0 LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 17)
                                                    
find_package(magic_enum CONFIG REQUIRED)

add_executable(simulation src/simulation/main.cpp)

target_link_libraries(simulation PRIVATE magic_enum::magic_enum)